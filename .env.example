DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# OpenAI API Key (required for AI summaries)
AI_INTEGRATIONS_OPENAI_API_KEY=your-openai-api-key-here

# REQUIRED: LLM gateway base URL (OpenRouter-style). All LLM calls route through
# this — without it the OpenAI SDK hits api.openai.com and the configured models
# (gemini-2.5-flash-lite, claude-sonnet-4) will not resolve.
AI_INTEGRATIONS_OPENAI_BASE_URL=https://your-gateway/v1

# Optional: Anthropic key for Claude fallback in tier-1 filtering
# AI_INTEGRATIONS_ANTHROPIC_API_KEY=your-anthropic-key
# Optional: Brave Search key for enrichment web search
# BRAVE_API_KEY=your-brave-key

# Tavily API Key (required for web search enrichment)
# Get your API key from https://tavily.com
# Pricing: 1,000 free searches/month, then $0.008/search
# Used to find real-time company headquarters and founder residence information
TAVILY_API_KEY=your-tavily-api-key-here

# Optional: ScrapingBee API key for web scraping
# SCRAPINGBEE_API_KEY=your-scrapingbee-api-key

# Optional: SendGrid API key for email notifications (deprecated - use Telegram)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Optional: Telegram bot token for notifications
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Optional: Public server URL for Telegram webhook mode
# When set alongside TELEGRAM_BOT_TOKEN, the bot uses webhook mode (Telegram pushes updates).
# When omitted, the bot falls back to polling mode (the bot pulls updates).
# Must be a publicly accessible HTTPS URL (e.g., https://your-domain.com)
# SERVER_URL=https://your-domain.com

# Optional: shared secret enforced on POST /api/telegram-webhook (Telegram echoes it back)
# TELEGRAM_WEBHOOK_SECRET=long-random-string
# Optional: shared secret required (header x-ingest-secret) on POST /api/browser-ingest
# BROWSER_INGEST_SECRET=long-random-string

# WebAuthn / passkey host config (set these for your own domain when self-hosting)
# WEBAUTHN_RP_NAME=Sensei
# WEBAUTHN_RP_ID=your-domain.com
# WEBAUTHN_ORIGIN=https://your-domain.com

# Server port (defaults to 5000)
PORT=5000
