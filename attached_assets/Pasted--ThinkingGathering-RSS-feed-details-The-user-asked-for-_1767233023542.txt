▶ThinkingGathering RSS feed details
The user asked for a list of RSS feeds, likely for the sites related to business they mentioned before, including Business Times, Straits Times, and others. I need to be accurate in providing verified RSS links since some sites may not have them or could require specific formatting. I’ll compile the list based on their earlier input, focusing on business sections and providing a note on the verification status for each feed.Clarifying RSS feed verification
I need to be clear that the RSS feeds I’m providing are verified to the best of my knowledge, but users should check them directly on the respective sites or use Replit to test them. I can suggest running Python code for fetching the Business Times RSS feeds, but since I can’t access the internet, I’ll base the links on known patterns. I’ll include multiple URLs from Business Times, such as those previously noted. It’s important to recommend verifying them.Compiling known RSS feeds
For the Straits Times, I can reference earlier feeds like https://www.straitstimes.com/news/business/rss.xml and https://www.straitstimes.com/singapore/rss.xml, plus others from their general feed page. For CNA, there’s the feed at https://www.channelnewsasia.com/rss, and previously noted ones like https://www.channelnewsasia.com/api/v1/rss-outbound-feed. Reuters has a general feed at https://www.reuters.com/rss and a better option for business news at https://www.reuters.com/rssFeed/businessNews. I’ll include other feeds for SCMP, Nikkei Asia, and Bloomberg as well, noting their specific URLs.Creating a verified RSS feeds list
I think a better approach is to compile a list of confirmed high-confidence RSS feeds for the user, such as those from Tech in Asia, DealStreetAsia, e27, and Reuters. I’ll mark which ones need verification and provide a simple code snippet to check if they work, which they can use in Replit. Since they asked for a clean list of feeds, I’ll include verification notes and next steps for adding them to the database, keeping everything concise but thorough.Compiling and verifying RSS feeds
I’m going to create a list of RSS feeds with their verification statuses, indicating whether they’re “Likely (high confidence)” or “Needs verification.” For those I’m confident about, I’ll include Tech in Asia, DealStreetAsia, and various feeds from Business Times, Straits Times, CNA business, and others, noting their confidence levels. I’ll also provide a Python script that uses feedparser to verify these feeds and insert them into an SQLite DB for Replit, along with clear instructions for the user to follow.
Below is a clean, focused list of RSS feeds you can add to your Replit DB (grouped by site). I mark my confidence level for each URL and include a short verification plan plus a ready-to-run Python snippet you can paste into Replit to verify each feed and bulk-insert working feeds into your SQLite sources/rss_feeds tables.
IMPORTANT: I cannot fetch the feeds from here — the confidence labels are based on common/official feed patterns and prior data. Use the verification script I provide to confirm each feed is live and returns entries.

Recommended RSS feeds (business / relevant sections)



Business Times (high confidence)

https://www.businesstimes.com.sg/rss/feeds/companies-markets
https://www.businesstimes.com.sg/rss/feeds/singapore
https://www.businesstimes.com.sg/rss/feeds/startups-tech
https://www.businesstimes.com.sg/rss/feeds/wealth



The Straits Times (medium-high)

https://www.straitstimes.com/news/business/rss.xml
https://www.straitstimes.com/singapore/rss.xml
(General list of ST feeds: https://www.straitstimes.com/RSS-Feeds — check for section-specific links)



CNA / Channel NewsAsia (high)

https://www.channelnewsasia.com/api/v1/rss-outbound-feed?_format=xml&category=6511  (Business)
https://www.channelnewsasia.com/api/v1/rss-outbound-feed?_format=xml&category=10416 (Singapore)



Reuters (medium)

https://www.reuters.com/tools/rss  (use the Reuters tools page to pick specific feeds)
Example business-ish feed: https://www.reuters.com/finance (verify via tools page or use the specific feed you need)



South China Morning Post (high)

https://www.scmp.com/rss/91/feed  (Business)
https://www.scmp.com/rss/2/feed   (Asia / general)



Nikkei Asia (medium)

https://asia.nikkei.com/rss/asia.xml
(Nikkei has more section feeds; verify on site)



Financial Times — Asia Pacific (medium)

https://www.ft.com/world/asia-pacific?format=rss



Tech in Asia (high)

https://www.techinasia.com/feed



DealStreetAsia (high)

https://www.dealstreetasia.com/feed



e27 (high)

https://e27.co/feed/



KrASIA (medium)

https://kr-asia.com/feed



Additional / regional or niche feeds (verify before use)

Alpha Southeast Asia: https://alphasoutheastasia.com/feed
Investvine Southeast Asia tag: https://investvine.com/tag/southeast-asia/feed/



Feeds to treat cautiously (paywalls / unreliable RSS formats)

Financial Times (some content behind paywall — headlines are fine, full text may be restricted)
Bloomberg / WSJ / FT (full articles often paywalled; RSS may give headlines or truncated content)
WSJ: paywalled and not suitable for full-text scraping without subscription


Verification plan (recommended)


For each feed:

Ensure HTTP 200
Ensure feedparser returns >0 entries
Extract sample title / link / published date
If feed returns empty, check feed URL on the official site (feed URLs change), or use site’s RSS landing page




Run this verification + import script in Replit


Add feedparser to requirements.txt (feedparser)
Ensure your Replit has internet access (it should for normal web requests)
Create a small script to test each feed and optionally insert valid feeds into your SQLite DB (or output SQL for you to run).

Paste this into a file (e.g., verify_feeds.py) in Replit and run it:
# verify_feeds.py
import feedparser
import sqlite3
from datetime import datetime

# List of feeds to verify
FEEDS = [
    # Business Times
    ("Business Times - Companies & Markets", "https://www.businesstimes.com.sg/rss/feeds/companies-markets"),
    ("Business Times - Singapore", "https://www.businesstimes.com.sg/rss/feeds/singapore"),
    ("Business Times - Startups & Tech", "https://www.businesstimes.com.sg/rss/feeds/startups-tech"),
    ("Business Times - Wealth", "https://www.businesstimes.com.sg/rss/feeds/wealth"),

    # Straits Times
    ("Straits Times - Business", "https://www.straitstimes.com/news/business/rss.xml"),
    ("Straits Times - Singapore", "https://www.straitstimes.com/singapore/rss.xml"),

    # CNA
    ("CNA - Business", "https://www.channelnewsasia.com/api/v1/rss-outbound-feed?_format=xml&category=6511"),
    ("CNA - Singapore", "https://www.channelnewsasia.com/api/v1/rss-outbound-feed?_format=xml&category=10416"),

    # Reuters (placeholder - verify via Reuters site/tools)
    ("Reuters - Business (verify on site)", "https://www.reuters.com/tools/rss"),

    # SCMP
    ("SCMP - Business", "https://www.scmp.com/rss/91/feed"),
    ("SCMP - Asia", "https://www.scmp.com/rss/2/feed"),

    # Nikkei Asia
    ("Nikkei Asia - Asia", "https://asia.nikkei.com/rss/asia.xml"),

    # FT Asia Pacific
    ("Financial Times - Asia Pacific", "https://www.ft.com/world/asia-pacific?format=rss"),

    # Tech in Asia
    ("Tech in Asia - Main", "https://www.techinasia.com/feed"),

    # DealStreetAsia
    ("DealStreetAsia - Main", "https://www.dealstreetasia.com/feed"),

    # e27
    ("e27 - Main", "https://e27.co/feed/"),

    # KrASIA
    ("KrASIA - Main", "https://kr-asia.com/feed"),

    # Extras
    ("Alpha Southeast Asia", "https://alphasoutheastasia.com/feed"),
    ("Investvine - Southeast Asia tag", "https://investvine.com/tag/southeast-asia/feed/"),
]

# Optional: path to your sqlite DB used by the app
SQLITE_DB_PATH = "app.db"  # change to your DB path

def test_feed(name, url):
    print(f"Testing: {name}\n  {url}")
    try:
        feed = feedparser.parse(url)
        status = feed.get('status', None)
        entries = feed.get('entries', [])
        if status and status != 200:
            print(f"  HTTP status: {status} (non-200)")
        if not entries:
            print("  ❌ No entries found (empty feed).")
            # show feed.bozo_exception if any
            if feed.get('bozo', False):
                print("  ⚠️ Feed parsing error:", feed.get('bozo_exception'))
            return False, 0
        # print sample entry
        first = entries[0]
        title = first.get('title', '<no title>')
        link = first.get('link', '<no link>')
        published = first.get('published', first.get('updated', ''))
        print(f"  ✅ OK - {len(entries)} entries. Sample: {title} | {published} | {link}")
        return True, len(entries)
    except Exception as e:
        print("  ❌ Exception while parsing:", e)
        return False, 0

def insert_feed_into_db(conn, source_name, feed_name, feed_url):
    """
    Inserts a source + rss_feed if not exists.
    Assumes tables: sources(id, name, domain, tier, active) and rss_feeds(id, source_id, name, url, active)
    This is a minimal example; adjust schema/names to match your app.
    """
    cursor = conn.cursor()
    # check source row
    cursor.execute("SELECT id FROM sources WHERE name = ?", (source_name,))
    row = cursor.fetchone()
    if row:
        source_id = row[0]
    else:
        # create a generic source entry (domain/tier unknown here)
        cursor.execute("INSERT INTO sources (name, domain, tier, active, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?)",
                       (source_name, '', 2, 1, datetime.utcnow(), datetime.utcnow()))
        source_id = cursor.lastrowid
    # check rss feed exists
    cursor.execute("SELECT id FROM rss_feeds WHERE source_id = ? AND url = ?", (source_id, feed_url))
    if cursor.fetchone():
        print("  Already in DB.")
        return
    cursor.execute("INSERT INTO rss_feeds (source_id, name, url, active, created_at) VALUES (?, ?, ?, ?, ?)",
                   (source_id, feed_name, feed_url, 1, datetime.utcnow()))
    conn.commit()
    print("  Inserted into DB.")

if __name__ == "__main__":
    results = []
    for name, url in FEEDS:
        ok, count = test_feed(name, url)
        results.append((name, url, ok, count))

    # Optional: insert successful feeds into DB
    keep = input("\nInsert successful feeds into DB? (y/N): ").strip().lower()
    if keep == 'y':
        conn = sqlite3.connect(SQLITE_DB_PATH)
        for name, url, ok, count in results:
            if ok:
                # split source name to find or create source
                # For simplicity use the part before the hyphen if present
                source_name = name.split(" - ")[0].strip()
                insert_feed_into_db(conn, source_name, name, url)
        conn.close()
        print("Done.")
    else:
        print("No DB changes made. Review results above.")


Next steps I can do for you


I can run a verification in Replit for you (if you want, paste the script there and run it). If you want, I can also generate an SQL migration to batch-insert confirmed feeds into your DB.
I can also produce a compact JSON file of sources + feeds ready for import.

Would you like:

A) The script modified to auto-detect source domains and set tiers automatically (I can add heuristics), or
B) An SQL file to insert the verified feeds into your app schema once you run the verification?

Which do you prefer?
