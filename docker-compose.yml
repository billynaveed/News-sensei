version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: news-sensei-db
    environment:
      POSTGRES_USER: newsuser
      POSTGRES_PASSWORD: newspass123
      POSTGRES_DB: newssensei
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U newsuser -d newssensei"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
